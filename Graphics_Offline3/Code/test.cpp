#include <bits/stdc++.h>

#define CAP 300
#define pi (2*acos(0.0))
using namespace std;



int main(){
    vector<int> vec;
    vec.push_back(23);
    vec.push_back(22);
    vec.push_back(88);
    vec.erase(vec.begin()+ 1);
    pair<double, double> p;
    p = make_pair(2.2, 3.3);
    cout<<p.first<<" "<<p.second<<endl;
    return 0;
}

